The NINES project

Requires the following Gems:

TODO

Uses the following Plugins for the specified functionality

TODO

Requires Wordpress for the News tab

Requires Solr

Note:
There are a number of new rake tasks under the namespace "collex:" which help with deployment.

In particular, the javascript and css files are compressed and merged into one so that the page loads faster.

To deploy, after getting the source from SVN, run:

	rake collex:update_site
