<% if @hit == nil -%>
Tagged object not found.
<% else -%>
<table>
	<tr>
		<td>
			resource:
		</td>
		<td>
			<a href="<%= @hit["url"] %>"><%= @hit["url"] %></a>
		</td>
	</tr>
	<%= render :partial => '/results/hit_body', :locals => { :hit => @hit, :row_id => 'row', :no_links => false } %>
</table>
<% end -%>
